rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Current app uses server-side Firebase Admin SDK.
    // Keep client writes closed by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
